@model dotnet01.Areas.Admin.Models.AccountNewViewModel
@{
    ViewBag.Title = "New";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<br />
<div class="panel panel-default">
    <div class=" panel-heading">
        Добавление аккаунта 
    </div>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div class=" panel-body">
            @Html.ValidationSummary(true, "", new { @class = "alert-danger" })
            @Html.HiddenFor(m => m.Account.Id)
            <div class="form-group">
                <label>Имя пользователя</label>
                @Html.TextBoxFor(m => m.Account.Login, new { @class = "form-control" })
            </div>
            <div class="form-group">
                <label>Адрес электронной почты</label>
                @Html.TextBoxFor(m => m.Account.Mail, new { @class = "form-control" })
            </div>
            <div class="form-group">
                <label>Пароль</label>
                @Html.TextBoxFor(m => m.Account.Password, new { @class = "form-control" })
            </div>
            <div class="form-group">
                <label>Роль</label>
                @Html.TextBoxFor(m => m.Account.Role, new { @class = "form-control" })
            </div>
        </div>
        <div class=" panel-footer">
            @Html.ActionLink("Назад к списку", "Index", new { @class = "btn btn-default" })
            <input type="submit" value="Сохранить" class="btn btn-primary" />
        </div>
    }
</div>
